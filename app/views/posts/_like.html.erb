<% if current_user.already_liked?(post) %>
          <td><%= link_to 'Unlike', like_path(current_user.likes.find_by(post_id: post.id)), method: :delete %></td>
        <% else %>
          <td><%= link_to 'Like', likes_path(post_id: post.id), method: :post %></td>
<% end %> 